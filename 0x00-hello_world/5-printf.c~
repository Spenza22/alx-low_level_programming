#include <stdio.h>
/*
 * main - This is the main function teaching how to use the printf() function
 *Return: And I have to document its return function.
 */
int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
