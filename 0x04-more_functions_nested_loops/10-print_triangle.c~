#include "main.h"
/**
 * _isdigit - checks for number/digits
 * @c: int variable used for test
 * Return: 1 or 0
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		/* code */
		return (1);
	}
	else
	{
		return (0);
	}
}
