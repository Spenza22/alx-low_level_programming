#include "main.h"
/**
 * _isupper - checks for uppercase letters
 * @c: int variable used for test
 * Return: 1 or 0
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		/* code */
		return (1);
	}
	else
	{
		return (0);
	}
}
