#include <stdio.h>

/**
 *main - The main entry point
 *Return: This returns 0
 */
int main(void)
{
	int alpha = 48;

	while (alpha <= 57)
	{
		putchar(alpha);
		alpha++;
	}
	putchar(10);
	return (0);

}
