#include <stdio.h>
/**
 *main - This is the entry point
 *Return: This will return 0
 */

int main(void)
{
	int alpha = 97;

	while (alpha <= 122)
	{
		putchar(alpha);
		alpha++;
	}

	putchar(10);
	return (0);

}
