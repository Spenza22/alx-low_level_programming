#include <stdio.h>
/**
 *main - This is the main enrty point
 *Return: Returns zero
 */
int main(void)
{
	int alpha = 0;

	while (alpha <= 9)
	{
		printf("%d", alpha);
		alpha++;
	}
	printf("\n");
	return (0);
}
