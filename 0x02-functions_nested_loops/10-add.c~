#include "main.h"
/**
 * add - Function to add 2 integers together, and return the result
 * Return: It returns the sum of both integers
 * @num1: Accepts the first number
 * @num2: Accepts the second number
 */

int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return(sum);
}
