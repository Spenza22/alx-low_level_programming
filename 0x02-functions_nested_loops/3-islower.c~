#include "main.h"

/**
 * print_alphabet_x10 - Checks for lowercase characters.
 *  Return: Always 0.
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		/* code */
		return (1);
	}
	else
	{
		return (0);
	}

}
